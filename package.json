{
  "name": "ajyal",
  "version": "2.0.0",
  "description": "AJYAL: Decentralized Education & Aid Platform (Sandbox/Testnet)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "exit 0",
    "test": "jest --runInBand || exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}